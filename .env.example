# NR Industries — local environment
# Supabase project: ywzwzrzlittjiosskmcy
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co

# ⚠️ FILL THIS IN: Supabase Dashboard -> Project Settings -> API -> "anon / public" key
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional — only needed for privileged server tasks (never exposed to the client).
# Dashboard -> Project Settings -> API -> "service_role" key
SUPABASE_SERVICE_ROLE_KEY=

# Public site origin (used in emails / OTP redirect links)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
